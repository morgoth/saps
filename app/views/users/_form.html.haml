= simple_form_for @user do |f|
  = f.input :login, :input_html => { :disabled => @user.new_record? ? false : true}
  = f.input :email
  = f.input :password
  = f.input :password_confirmation
  = f.button :submit, t("confirm"), :class => "button"
