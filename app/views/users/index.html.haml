%h1= "users"[:users]
= link_to "New user"[:new_user], signup_path
%br
- for user in @users
  = user.login
  = link_to_if current_user==user,  'Change password'[:change_password], edit_user_path(user)
  = link_to 'Destroy'[:destroy], user, :confirm => 'Are you sure?'[:are_you_sure], :method => :delete
  %br
