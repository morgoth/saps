= simple_form_for @user, :url => account_path do |f|
  = f.input :login
  = f.input :email
  = f.input :password
  = f.input :password_confirmation
  = f.button :submit, t("confirm"), :class => "button"
