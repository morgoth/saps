%h1 Editing round

- form_for(:round, :url => league_round_path( params[:league_id], @round ), :html => {:method => :put}) do |f|
  = f.error_messages

  %p
    = f.label :name
    %br
    = f.text_field :name
  %p
    = f.label :date
    %br
    = f.datetime_select :date
  %p
    = f.submit "Update"

= link_to 'Back', league_rounds_path(@league)
