%h1= t("users")
= link_to t("views.users.new"), new_user_path
%ul#users
  - @users.each do |user|
    %li
      = user.login.capitalize
      = link_to t("destroy"), user, :confirm => t("sure"), :method => :delete
