%h1
  = link_to "Slideshow", {:controller => 'galleries', :action => 'slideshow', :link => "#{@photos[:slideshow]}"}, {:popup => true}
%p
  - @photos[:title].each_with_index do |photo, index|
    =link_to( image_tag( "#{@photos[:thumbnail][index]}"),"#{@photos[:photo][index] }")
