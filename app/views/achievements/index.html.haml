%h1= t("achievements")
- if logged_in?
  = link_to t("views.achievements.new"), new_achievement_path
  %br
  %br
%ul#achievements
  - @achievements.each do |achievement|
    %li
      .place= t("views.achievements.place", :place => achievement.place)
      .description= textilize achievement.description
      .played_at= achievement.played_at
      .photo= image_tag achievement.photo_url if achievement.photo_url.present?
